# Streamlit Cloud optimized requirements
# Minimal dependencies for dashboard and trading features

# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0

# Database
supabase>=2.18.1
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.7

# Data processing
pandas>=2.0.0
numpy>=1.24.0,<2.0.0

# Visualization
plotly>=5.17.0

# Trading dependencies (essential)
# alpaca-py dependencies - explicit to avoid conflicts
sseclient-py>=1.7.2,<2.0.0
websockets>=11.0.3,<12.0.0
msgpack>=1.0.3,<2.0.0
pydantic>=2.0.3,<3.0.0
pydantic-settings>=2.1.0
alpaca-py>=0.42.0
yfinance>=0.2.18

# Dashboard
streamlit>=1.50.0
streamlit-autorefresh>=1.0.1

# Utilities
beautifulsoup4>=4.12.0
aiohttp>=3.9.0
httpx>=0.28.0

# ML dependencies (lightweight - no torch to avoid timeout)
scikit-learn>=1.3.0
# Note: torch, pytorch-lightning excluded for Streamlit Cloud (too large)
# They are available in local development via pyproject.toml
