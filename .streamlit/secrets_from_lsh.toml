# Streamlit Cloud Secrets - Generated from LSH
# To use: Copy to Streamlit Cloud dashboard → Settings → Secrets

# Pull latest from LSH with:
# export LSH_SECRETS_KEY=fdc6fca3488483b15315e9512083c26de2e973671f3211cb6015bb86ba09fe02
# lsh lib secrets show --env mcli-streamlit-prod

# Or pull directly to file:
# cd .streamlit && lsh lib secrets pull --env mcli-streamlit-prod --output secrets.toml

[secrets]
# Supabase will be pulled from LSH
# Use: lsh lib secrets show --env mcli-streamlit-prod
